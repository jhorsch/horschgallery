


<div class="ribbon container_backdrop"><h4>Customers Also Viewed</h4>
  <% @photos.where(sub_cat: "#{@photo.sub_cat.downcase}").shuffle.take(8).each do |photo| %>
    <%= link_to image_tag(photo.image_url, class: "poster-container"), photo_url(photo.id) %>
  <% end %>
</div>


